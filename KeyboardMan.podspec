Pod::Spec.new do |p|
  p.name = 'KeyboardMan'
  p.version = '0.2'
  p.platform = :ios
  p.source = { :git => 'https://github.com/nixzhu/KeyboardMan.git' }
  p.source_files = 'KeyboardMan/*.swift'
end
